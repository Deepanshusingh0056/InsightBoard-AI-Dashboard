services:
  - type: web
    name: Autonomix-Assignment
    env: python
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
